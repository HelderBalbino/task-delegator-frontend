import Form from './form';
import { register } from '../../services/userService';

function Register() {
    const handleRegister = async (values: {
        name: string;
        email: string;
        password: string;
        sector_name: string;
        company_name: string;
    }) => {     }

